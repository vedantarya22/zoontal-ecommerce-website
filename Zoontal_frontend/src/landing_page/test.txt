<nav
      class="navbar navbar-expand-lg border-bottom "
      style={{ backgroundColor: "white" }}
    >
      <div class="container flex-column">
        <Link class="navbar-brand mx-auto mb-2" to="/">
          <img src="\Untitled.png" alt="logo" style={{ width: "120px" }} />
        </Link>

        <button
          class="navbar-toggler mb-2"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        {/* bottom row */}
        <div
          class="collapse navbar-collapse justify-content-center"
          id="navbarSupportedContent"
        >
          <form className="d-flex">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item ">
                <Link
                  className={`nav-link animated-link ${
                    isActive("/") ? "active" : ""
                  }`}
                  aria-current="page"
                  to="/"
                >
                  Home
                </Link>
              </li>
              <li class="nav-item ">
                <Link  
                className={`nav-link animated-link ${isActive("/collection") ? "active" : ""}`}
                 aria-current="page" to="/collection">
                  Collection
                </Link>
              </li>
              <li class="nav-item">
                <Link 
                  className={`nav-link animated-link ${isActive("/about") ? "active" : ""}`}
                 aria-current="page" to="/about">
                  About
                </Link>
              </li>
              <li class="nav-item ">
                <Link   
                className={`nav-link animated-link ${isActive("/blogs") ? "active" : ""}`}
                 aria-current="page" to="/blogs">
                  Blogs
                </Link>
              </li>
            </ul>
          </form>
        </div>
      </div>
    </nav>